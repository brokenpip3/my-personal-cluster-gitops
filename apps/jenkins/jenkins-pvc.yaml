apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: jenkins-aur-packages
  name: jenkins-aur-packages
  namespace: jenkins
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
